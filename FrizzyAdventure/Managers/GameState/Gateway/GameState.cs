namespace FrizzyAdventure.Managers.GameState.Gateway
{
    using FrizzyAdventure.Managers.GameState.Model;

    internal sealed class GameState
    {
        private PlayerState _playerState = new PlayerState();

        public GameState()
        {
        }
    }
}